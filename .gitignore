node_modules
.next
.DS_Store
*.tsbuildinfo
.env
.env.local
.env.*.local
dist
build
out
.vercel

# Per-student session artifacts — regenerated whenever the app is used
# against test-study/. Existing tracked example logs stay tracked; this
# only prevents new log files and photo uploads from cluttering git status.
test-study/logs/*.json
test-study/logs/photos/
test-study/feedback/
